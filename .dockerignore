node_modules
dist
.git
.idea
.claude
tasks

# Секреты / локальные env — в образ не должны попадать никогда.
.env
.env.*
!.env.example

# Документация и служебные файлы — не нужны в рантайме.
*.md
!README.md
LICENSE
.DS_Store
npm-debug.log*

# Docker/CI файлы сами по себе тоже не нужны внутри образа.
Dockerfile*
docker-compose*.yml
Caddyfile
